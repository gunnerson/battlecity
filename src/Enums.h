#pragma once

enum Dir {
  up,
  left,
  down,
  right,
};

enum Color {
  yellow,
  gray,
  green,
  purple,
};

enum TankType {
  player,
  sergeant,
  colonel,
  general,
  basic,
  fast,
  power,
  armor,
};
