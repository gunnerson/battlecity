#pragma once

enum Dir {
  up,
  left,
  down,
  right,
};

enum Color {
  yellow,
  gray,
  green,
  red,
};

enum TankType {
  player,
  sergeant,
  colonel,
  general,
  basic,
  fast,
  power,
  armor,
};

enum UpgradeType {
  helmet,
  freeze,
  shovel,
  star,
  grenade,
  life,
};
